<!DOCTYPE html>
<html>

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/css/normalize.min.css">
    <link rel="stylesheet" href="/css/main.css">
    <script src="https://code.jquery.com/jquery-3.6.0.min.js" integrity="sha256-/xUj+3OJU5yExlq6GSYGSHk7tPXikynS7ogEvDej/m4=" crossorigin="anonymous"></script>
    <title>Forking Cocktails</title>
</head>

<body>
    <header> 
        <nav id="sessions-nav">
            <% if(currentUser) { %>
                <a href="/">Home</a>
                <form action="/sessions?_method=DELETE" method="POST">
                    <input type="submit" value="Log Out" />
                </form>
            <% } else { %>
                <a href="/">Home</a>
                <a href="/creators/new">Register</a>
                <a href="/sessions/new">Login</a>
            <% } %>
        </nav>
        <nav class="topNav">
            <ul id="db-nav">
                <li>
                    <a id="authors" href="/creators">Creators</a>
                </li>
                <li class="plus">
                    +
                </li>
                <li>
                    <a id="cocktails" href="/cocktails">Cocktails</a>
                </li>
                <li class="plus">
                    +
                </li>
                <li>
                    <a id="ingredients" href="/ingredients">Ingredients</a>
                </li>
            </ul>
        </nav>
        <h2>Forking Cocktails</h2>
    </header>

    <main>
        <h3>Cocktail Page</h3>
        <form action="/cocktails/<%=cocktail._id%>?_method=DELETE" method="POST">
            <h4>Name</h4>
            <p><%= cocktail.name %></p>
            <h4>Created By</h4>
            <p><%= cocktail.createdBy.name %></p>
            <h4>Description</h4>
            <p><%= cocktail.description %></p>
            <h4>Ingredients</h4>
            <ul id="show-ingredients">
                <% for (let i = 0; i < cocktail.recipe.length; i++) { %>
                    <li><%= cocktail.recipe[i].number + cocktail.recipe[i].fraction + " " + cocktail.recipe[i].unit + " " + cocktail.recipe[i].ingredient.name %> </li>
                <% } %>
            </ul>
          
            <h4>Method</h4>
            <p><%= cocktail.method %></p>
            <h4>Glass</h4>
            <p><%= cocktail.glass %></p>
            <h4>Garnish</h4>
            <p><%= cocktail.garnish %></p>
            <h4>Notes on Preparation</h4>
            <p><%= cocktail.notes %></p>
            
            <% if (cocktail.parent) { %>
                <h4>Forked FROM</h4>
                <p><a href="<%=cocktail.parent._id%>"><%=cocktail.parent.name%></a></p>
            <% } %>
            
            <% if (children.length) { %>
                <h4>Forked BY</h4>
                <ul id="show-children">
                    <% children.forEach(child => { %>
                        <li><a href="/cocktails/<%= child._id %>"><%=child.name%></a></li>
                    <% }) %>
                </ul>
            <% } %>
        <% if (currentUser) { %>    
        <% if (currentUser._id == cocktail.createdBy._id){ %>                     
            <a id="cocktail-edit"class="actions" href="/cocktails/<%=cocktail._id%>/edit">Edit Cocktail</a>
            <input type="submit" value="Delete Cocktail" id="cocktail-delete"/>
        <% } %>
        
            <a id="cocktail-fork" class="actions" href="/cocktails/<%=cocktail._id%>/fork">Fork Cocktail</a>
       <% } %>

        </form>
    </main>
</body>

</html>