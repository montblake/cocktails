<!DOCTYPE html>
<html>

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/css/normalize.min.css">
    <link rel="stylesheet" href="/css/main.css">
    <script src="https://code.jquery.com/jquery-3.6.0.min.js" integrity="sha256-/xUj+3OJU5yExlq6GSYGSHk7tPXikynS7ogEvDej/m4=" crossorigin="anonymous"></script>
    <title>Forking Cocktails</title>
</head>

<body>
    <header> 
        <nav class="topNav cocktails">
            <ul>
                <li>
                    <a href="/">Home</a>
                </li>
                <li>
                    <a href="/cocktails">Cocktail Index</a>
                </li>
            </ul>
        </nav>
        <h2>Forking Cocktails</h2>
    </header>

    <main>
        <h3>Cocktail Page</h3>
        <form action="/cocktails/<%=cocktail._id%>?_method=DELETE" method="POST">
            <h4>Name</h4>
            <p><%= cocktail.name %></p>
            <h4>Created By</h4>
            <p><%= cocktail.createdBy.name %></p>
            <h4>Description</h4>
            <p><%= cocktail.description %></p>
            <h4>Ingredients</h4>
            <ul id="show-ingredients">
                <% for (let i = 0; i < cocktail.recipe.length; i++) { %>
                    <li><%= cocktail.recipe[i].number + cocktail.recipe[i].fraction + " " + cocktail.recipe[i].unit + " " + cocktail.recipe[i].ingredient %> </li>
                <% } %>
            </ul>
          
            <h4>Method</h4>
            <p><%= cocktail.method %></p>
            <h4>Glass</h4>
            <p><%= cocktail.glass %></p>
            <h4>Garnish</h4>
            <p><%= cocktail.garnish %></p>
            <h4>Notes on Preparation</h4>
            <p><%= cocktail.notes %></p>
            
            <% if (cocktail.parent) { %>
                <h4>Cocktail from which <%=cocktail.createdBy.name%>'s <%=cocktail.name%> was Forked</h4>
                <p><a href="<%=cocktail.parent._id%>"><%=cocktail.parent.name%></a></p>
            <% } %>
            
            <% if (cocktail.children.length) { %>
                <h4>Cocktails Forked from <%=cocktail.createdBy.name%>'s <%=cocktail.name%></h4>
                <ul>
                    <% cocktail.children.forEach(child => { %>
                        <li><%=child.name%></li>
                    <% }) %>
                </ul>
            <% } %>
            
                            
            <a id="cocktail-edit"class="actions" href="/cocktails/<%=cocktail._id%>/edit">Edit Cocktail</a>
            <input type="submit" value="Delete Cocktail" id="cocktail-delete"/>
            <a id="cocktail-fork" class="actions" href="/cocktails/<%=cocktail._id%>/fork">Fork Cocktail</a>
        </form>
    </main>
</body>

</html>