<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/css/normalize.min.css">
    <link rel="stylesheet" href="/css/main.css">
    <script src="https://code.jquery.com/jquery-3.6.0.min.js" integrity="sha256-/xUj+3OJU5yExlq6GSYGSHk7tPXikynS7ogEvDej/m4=" crossorigin="anonymous"></script>
    <title>Forking Cocktails</title>
</head>
<body>
    <nav>
        <form action="/cocktails/<%= cocktail._id %>?_method=DELETE" method="POST">
            <input type="submit" value="delete" />
        </form>
    </nav>
    <h1><%=cocktail.name%></h1>
    <h2>Created by <%= cocktail.createdBy.name %></h2>
    <h3><%= cocktail.method %></h3>
    <ul class="ingredients">
        <% cocktail.recipe.forEach(recipeLine => { %>
            <li><%= recipeLine.measurement + recipeLine.fraction + " " + recipeLine.unit + " " + recipeLine.ingredient %></li>
        <% }); %>
    </ul>
</body>
</html>